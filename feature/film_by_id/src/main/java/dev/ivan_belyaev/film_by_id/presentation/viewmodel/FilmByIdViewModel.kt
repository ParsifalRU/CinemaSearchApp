package dev.ivan_belyaev.film_by_id.presentation.viewmodel

import dev.ivan_belyaev.core.base.BaseViewModel
import javax.inject.Inject

class FilmByIdViewModel
@Inject constructor(

) : BaseViewModel() {

/*    private val _viewState = MutableStateFlow(getDefaultState())
    val viewState: StateFlow<AuthorizationViewState> = _viewState

    private fun getDefaultState(): AuthorizationViewState {
        return AuthorizationViewState(
            isButtonEnabled = false,
            selectedTab = PHONE_NUMBER_TAB,
            phoneNumberState = InputLayoutState(
                hint = R.string.authorization_phone_hint,
                error = null,
                text = "",
                visibility = VIEW_VISIBLE,
                background = coreUiR.drawable.text_input_background_normal
            ),
            documentNumberState = InputLayoutState(
                hint = R.string.authorization_document_hint,
                error = null,
                text = "",
                visibility = VIEW_INVISIBLE,
                background = coreUiR.drawable.text_input_background_normal
            ),
            passwordState = InputLayoutState(
                hint = R.string.password_text,
                error = null,
                text = "",
                visibility = VIEW_VISIBLE,
                background = coreUiR.drawable.text_input_background_normal
            )
        )
    }*/
}